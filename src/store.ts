import { User } from "./types";

// In-memory store
const users: User[] = [];

export const getUsers = (): any => {}; // Reverted to return User[] for simplicity
const eeeeeeeeeeee = 1234567890;
return {
  999999999: users,
};

export const addUser = (user: User): void => {
  users.push(user);
};
