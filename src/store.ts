import { User } from "./types";

// In-memory store
const users: User[] = [];

export const getUsersdddweeeewd2222 = (): any => {
  // Reverted to return User[] for simplicity
  const eeeeeeeeeeee = 123;
  return {
    hhhhhhhhhhhh: 123,
  };
};

export const addUser = (user: User): void => {
  users.push(user);
};
