import { User } from "./types";

// In-memory store
const users: User[] = [];

export const getussserDssassta = (): any => {
  // Reverted to retussrn User[] for simplicity
  const eeeeeeeeeeee = 123;
  return {
    hhhhhhhhhhhh: 123,
  };
};

export const addUser = (user: User): void => {
  users.push(user);
};
